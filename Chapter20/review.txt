Review

[1] What is a container?
class templates in the STL that store and manage collections of elements

[2] What containers does the STL provide?
Array, vector, Deque, List, Forward List

[3] What is an associative container? Give at least three examples.
Set, Map, Multiset, Multimap

[4] Is list an associative container? Why not?
No it isn't because associative containers can store data in a sorted manner and
more importantly can look up values based on keys

[5] What is a hash function?
Takes a given input and scrambles it using a certain algorithm like SHA-256
The output will be deterministic and ideally irreversible, this is useful
to prevent the same input generating the same output

[6] What is the basic ordering property of binary tree?
every node has exactly 2 children or none and is considered a leaf
The nodes can be red or black

[7] What (roughly) does it mean for a tree to be balanced?
When each subtree has approximately as many nodes as every other subtree that's equally far from the root

[8] How much space per element does a map take up?
- The data (std::pair<key, value>
- Parent pointer (8 bytes)
- Left Child pointer (8 bytes)
- Right Child pointer (8 bytes)
- Color bit (typically 1-8 bytes)

[9] How much space per element does a vector take up?
a std::vector<std::pair<int,int>> takes up 8 bytes for key+value per element.
The total overhead of vector is 24 bytes

[10] Why would anyone use an unordered_map when an (ordered) map is available?

[11] What is a big-O measure?

[12] How does a set differ from a map?

[13] How does a multimap differ from a map?

[14] How does an associative container differ from other containers?

[15] Why is an array not a container?

[16] Why bother to time code?

[17] How do you time a piece of code?

[18] When does structured binding clarify code? When does it obscure it?

[19] What containers does the STL offer?

[20] What is an iterator category?

[21] What kinds of iterators does the STL offer? Mention at least eight.

[22] What operations are provided by a random-access iterator, but not a bidirectional iterator?

Terms

associative container
map
associative array
structured binding
balanced tree
unordered_map
big-O
duration
now()
time_point
clock
date
set
container
almost container
array
istream_iterator
ostream_iterator
range
iterator category
random-access iterator
output range
hash function
ranges::
